// Settings

enum SettingsOptionType {
  labelAndValue,
  labelAndCustomWidget,
  buttonTextOneSide,
  labelSwitchable,
  labelNavigable,
  labelSelectable
}

enum OptionButtonOneSide {
  left,
  right,
}
