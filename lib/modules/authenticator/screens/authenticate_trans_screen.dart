import "package:flutter/material.dart";

class AuthenticateTransactionScreen extends StatefulWidget {
  const AuthenticateTransactionScreen({Key? key}) : super(key: key);

  @override
  State<AuthenticateTransactionScreen> createState() =>
      _AuthenticateTransactionScreenState();
}

class _AuthenticateTransactionScreenState
    extends State<AuthenticateTransactionScreen> {
  @override
  Widget build(BuildContext context) {
    return Container(child: Text("Hello"));
  }
}
